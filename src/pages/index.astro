---
import Layout from '../layouts/Layout.astro';
import { data } from '../data/info.js';

const lang = "es";
const t = data[lang];
---

<Layout title={`${t.infoPersonal.nombre_corto} | Portfolio`} lang={lang}>
    <header class="navbar">
        <div class="container navbar__content">
            <a href="#inicio" class="navbar__brand">
                {t.infoPersonal.iniciales} / {t.infoPersonal.nombre_corto}
            </a>

            <nav class="navbar__links">
                <a href="#inicio">{t.nav.inicio}</a>
                <a href="#proyectos">{t.nav.proyectos}</a>
                <a href="#habilidades">{t.nav.habilidades}</a>
                <a href="#formacion">{t.nav.formacion}</a>
                <a href="#contacto">{t.nav.contacto}</a>
                
                <div class="controls">
                    <button id="themeToggle" class="theme-toggle" aria-label="Toggle theme">
                        <span id="themeIcon">🌓</span>
                    </button>
                    <a href="/en" class="lang-toggle">EN</a>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <!-- Hero Section -->
        <section id="inicio" class="hero">
            <div class="container hero__grid">
                <div class="hero__content">
                    <span class="tag">{t.infoPersonal.tag}</span>
                    <h1>
                        {t.infoPersonal.heroTitle} <span>{t.infoPersonal.heroTitleAccent}</span>.
                    </h1>
                    <p>{t.infoPersonal.descripcion}</p>
                    <div class="hero__actions">
                        <a href="#proyectos" class="btn btn--primary">{t.nav.proyectos}</a>
                        <a href={t.infoPersonal.github} target="_blank" rel="noopener" class="btn btn--outline">GitHub</a>
                        <a href={t.infoPersonal.linkedin} target="_blank" rel="noopener" class="btn btn--outline">LinkedIn</a>
                    </div>
                    <div class="hero__stats">
                        {t.infoPersonal.stats.map(stat => (
                            <div>
                                <strong>{stat.val}</strong>
                                <span>{stat.label}</span>
                            </div>
                        ))}
                    </div>
                </div>

                <div class="profile-card">
                    <div class="profile-img-container">
                        <img src="/perfil.jpeg" alt={t.infoPersonal.nombre} />
                    </div>
                    <div class="profile-info-overlay">
                        <h3>{t.infoPersonal.nombre}</h3>
                        <p>{t.infoPersonal.especialidad}</p>
                        <p style="font-size: 0.85rem; color: var(--text-muted); margin-top: 0.5rem;">{t.infoPersonal.ubicacion}</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- About Section -->
        <section class="section">
            <div class="container section__header">
                <h2 class="section__title">{t.infoPersonal.aboutTitle}</h2>
                <div class="section__text">
                    <p style="font-size: 1.5rem; font-weight: 500; color: var(--text-primary); line-height: 1.4;">
                        {t.infoPersonal.aboutText}
                    </p>
                </div>
            </div>
        </section>

        <!-- Projects Section -->
        <section id="proyectos" class="section">
            <div class="container">
                <div class="section__header">
                    <span class="tag">Selected Work</span>
                    <h2 class="section__title">{t.infoPersonal.projectsTitle}</h2>
                </div>
                
                <div class="projects-list">
                    {t.proyectos.map((proyecto) => (
                        <article class="project-item">
                            <div class="project-meta">
                                <span class="tag">{proyecto.estado}</span>
                                <h3>{proyecto.nombre}</h3>
                                <span class="period">{proyecto.periodo}</span>
                                <p style="font-weight: 600; color: var(--accent-brand); margin-bottom: 1rem;">{proyecto.tipo}</p>
                            </div>
                            
                            <div class="project-content">
                                <p>{proyecto.descripcion}</p>
                                
                                <div class="project-impact">
                                    <strong>Key Impact</strong>
                                    {proyecto.impacto}
                                </div>
                                
                                <div class="project-stack">
                                    {proyecto.tecnologias.map((tecnologia) => (
                                        <span>{tecnologia}</span>
                                    ))}
                                </div>
                                
                                <div style="margin-top: 2rem;">
                                    <a href={proyecto.github} target="_blank" rel="noopener" class="btn btn--outline" style="padding: 10px 20px; font-size: 0.9rem;">
                                        View Repository →
                                    </a>
                                </div>
                            </div>
                        </article>
                    ))}
                </div>
            </div>
        </section>

        <!-- Skills Section -->
        <section id="habilidades" class="section">
            <div class="container">
                <div class="section__header">
                    <h2 class="section__title">{t.infoPersonal.skillsTitle}</h2>
                </div>
                
                <div class="skills-modern-grid">
                    {t.habilidades.map((habilidad) => (
                        <div class="skill-category">
                            <h3>{habilidad.grupo}</h3>
                            <div class="skill-list">
                                {habilidad.items.map((item) => (
                                    <div class="skill-item">{item}</div>
                                ))}
                            </div>
                        </div>
                    ))}
                </div>
            </div>
        </section>

        <!-- Education & Certs -->
        <section id="formacion" class="section">
            <div class="container">
                <div class="section__header">
                    <span class="tag">Academic & Certifications</span>
                    <h2 class="section__title">{t.infoPersonal.educationTitle}</h2>
                </div>

                <div class="education-layout">
                    <!-- Main Education -->
                    <div class="education-main">
                        <article class="edu-item">
                            <h3>{t.educacion.carrera}</h3>
                            <span class="uni">{t.educacion.universidad}</span>
                            <div class="meta">
                                <span>{t.educacion.periodo}</span> • <span>{t.educacion.estado}</span>
                            </div>
                        </article>
                        
                        <div class="info-block">
                            <h3>{lang === 'es' ? 'Idiomas' : 'Languages'}</h3>
                            <div class="lang-list">
                                {t.idiomas.map((idioma) => (
                                    <div class="lang-card">
                                        <strong>{idioma.nombre}</strong>
                                        <span>{idioma.nivel}</span>
                                    </div>
                                ))}
                            </div>
                        </div>
                    </div>

                    <!-- Sidebar: Certifications -->
                    <div class="sidebar-info">
                        <div class="info-block">
                            <h3>Professional Certifications</h3>
                            <div class="cert-list">
                                {t.certificaciones.map((cert) => (
                                    <article class="cert-item">
                                        <h4>{cert.nombre}</h4>
                                        <p>{cert.institucion} • {cert.fecha}</p>
                                    </article>
                                ))}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Contact Section -->
        <section id="contacto" class="section" style="border-bottom: none;">
            <div class="container">
                <div class="contact-cta">
                    <span class="tag">Get in touch</span>
                    <h2>{t.infoPersonal.contactTitle}</h2>
                    <p style="color: var(--text-secondary); font-size: 1.25rem;">{t.infoPersonal.contactDesc}</p>
                    
                    <div class="contact-grid">
                        <a href={`mailto:${t.infoPersonal.email}`} class="contact-card">
                            <span style="font-size: 2rem;">✉️</span>
                            <strong>Email</strong>
                            <span>{t.infoPersonal.email}</span>
                        </a>
                        <a href={`tel:${t.infoPersonal.telefono}`} class="contact-card">
                            <span style="font-size: 2rem;">📞</span>
                            <strong>Phone</strong>
                            <span>{t.infoPersonal.telefono}</span>
                        </a>
                        <a href={t.infoPersonal.linkedin} target="_blank" rel="noopener" class="contact-card">
                            <span style="font-size: 2rem;">💼</span>
                            <strong>LinkedIn</strong>
                            <span>Professional Profile</span>
                        </a>
                        <a href={t.infoPersonal.github} target="_blank" rel="noopener" class="contact-card">
                            <span style="font-size: 2rem;">💻</span>
                            <strong>GitHub</strong>
                            <span>Code Repositories</span>
                        </a>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>© 2026 {t.infoPersonal.nombre_corto}.</p>
        </div>
    </footer>
</Layout>

<script>
    const themeToggle = document.getElementById('themeToggle');
    const themeIcon = document.getElementById('themeIcon');
    
    const updateIcon = (theme) => {
        if (themeIcon) {
            themeIcon.innerText = theme === 'dark' ? '☀️' : '🌙';
        }
    };

    const currentTheme = document.documentElement.getAttribute('data-theme');
    updateIcon(currentTheme);

    themeToggle?.addEventListener('click', () => {
        const currentTheme = document.documentElement.getAttribute('data-theme');
        const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
        document.documentElement.setAttribute('data-theme', newTheme);
        localStorage.setItem('theme', newTheme);
        updateIcon(newTheme);
    });
</script>
